spring:
  cloud:
    config:
      uri: http://localhost:9090
      name: config
      profile: server
server:
  port: 9999
management:
  endpoints:
    web:
      exposure:
        #暴露那些端口  *表示全部
        include: "*"